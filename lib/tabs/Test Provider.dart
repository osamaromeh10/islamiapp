import 'package:flutter/material.dart';

class ProviderTest extends StatelessWidget {
  static const String routeName = " TestProvider";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Provider", style: TextStyle(fontSize: 20),)),


    );
  }
}
